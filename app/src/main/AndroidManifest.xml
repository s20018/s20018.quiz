<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="jp.ac.it_college.s20003.quiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        //android:theme="@style/Theme.Quiz"//
        tools:ignore="ExtraText">
        <activity android:name="com.example.s20018quiz.ResultActivity"></activity>
        <activity android:name="com.example.s20018quiz.QuizActivity" />
        <activity android:name="com.example.s20018quiz.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>